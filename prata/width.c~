/* поля разной ширины */
#include <stdio.h>
#define PAGES 959

int main(void)
{
    printf("*%d*\n", PAGES);
    printf("*%d*\n", PAGES);
    printf("*%d*\n", PAGES);
    printf("*%d*\n", PAGES);

    return 0;
}
